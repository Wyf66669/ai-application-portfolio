# 面向对象基础

## 封装
封装是将数据和对数据的操作封装在一起，对外隐藏内部实现细节，只暴露必要接口。通过访问控制（如 private / public）保护对象状态，提高安全性和可维护性。

## 继承
继承允许子类复用父类的属性和方法，并可以扩展或覆盖行为。有利于代码复用和层次化建模，但需注意过深继承带来的耦合。

## 多态
多态指同一接口在不同对象上可表现出不同行为。常见实现包括方法重写与接口实现，使调用方依赖抽象而非具体类。
